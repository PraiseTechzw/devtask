import FontAwesome from '@expo/vector-icons/FontAwesome'; import { useClerk, useUser } from '@clerk/expo'; import { useRouter } from 'expo-router'; import { Pressable, StyleSheet, Text, View } from 'react-native'; import { AppShell, SectionTitle } from '@/components/app-shell'; import { FontFamily, Palette } from '@/constants/theme';
export default function SettingsScreen() { const { user } = useUser(); const { signOut } = useClerk(); const router = useRouter(); const logout = async () => { await signOut(); router.replace('/welcome'); }; return <AppShell title="Settings"><View style={styles.profile}><View style={styles.avatar}><Text style={styles.avatarText}>{user?.firstName?.[0] || 'P'}</Text></View><View><Text style={styles.name}>{user?.fullName || 'Praise'}</Text><Text style={styles.email}>{user?.primaryEmailAddress?.emailAddress || 'you@example.com'}</Text></View></View><SectionTitle>Reminders</SectionTitle><Row icon="bell-o" label="Daily nudge time" value="08:30 AM" /><Row icon="mobile" label="Push notifications" value="On" /><SectionTitle>Appearance</SectionTitle><Row icon="moon-o" label="Dark mode" value="On" /><SectionTitle>Data</SectionTitle><Row icon="github" label="GitHub connection" value="Connect" /><Row icon="download" label="Export data" value="" /><Pressable accessibilityRole="button" onPress={logout} style={styles.logout}><Text style={styles.logoutText}>Sign out</Text></Pressable></AppShell>; }
function Row({ icon, label, value }: { icon: string; label: string; value: string }) { return <View style={styles.row}><FontAwesome color="#88C7FF" name={icon as never} size={18} /><Text style={styles.rowLabel}>{label}</Text><Text style={styles.rowValue}>{value}</Text><FontAwesome color="#6193C8" name="chevron-right" size={11} /></View>; }
const styles = StyleSheet.create({ profile: { padding: 16, flexDirection: 'row', alignItems: 'center', gap: 12, borderRadius: 16, borderWidth: 1, borderColor: '#0B5BA2', backgroundColor: '#062549' }, avatar: { width: 47, height: 47, borderRadius: 24, alignItems: 'center', justifyContent: 'center', backgroundColor: '#087FFF' }, avatarText: { color: '#FFF', fontFamily: FontFamily.bold, fontSize: 19 }, name: { color: '#F2F8FF', fontFamily: FontFamily.semibold, fontSize: 16 }, email: { marginTop: 3, color: '#8FB5E6', fontFamily: FontFamily.regular, fontSize: 12 }, row: { minHeight: 55, paddingHorizontal: 13, flexDirection: 'row', alignItems: 'center', gap: 12, borderBottomWidth: 1, borderBottomColor: '#113860', backgroundColor: '#062549' }, rowLabel: { flex: 1, color: '#E5F2FF', fontFamily: FontFamily.regular, fontSize: 14 }, rowValue: { color: Palette.cyan, fontFamily: FontFamily.medium, fontSize: 12 }, logout: { marginTop: 30, alignItems: 'center', paddingVertical: 15, borderRadius: 14, borderWidth: 1, borderColor: '#A9424A' }, logoutText: { color: '#FF8790', fontFamily: FontFamily.semibold, fontSize: 14 } });
