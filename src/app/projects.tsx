import { useRouter } from 'expo-router'; import { Pressable, StyleSheet, Text, View } from 'react-native'; import { AppShell, ProjectCard } from '@/components/app-shell'; import { projects } from '@/constants/mock-data'; import { Palette, FontFamily } from '@/constants/theme';
export default function ProjectsScreen() { const router = useRouter(); return <AppShell title="Projects" action={<Pressable onPress={() => router.push('/add-project')}><Text style={styles.add}>+</Text></Pressable>}><View style={styles.filters}>{['All', 'Active', 'Slowing', 'Stalled'].map((item, index) => <View key={item} style={[styles.filter, index === 0 && styles.selected]}><Text style={[styles.filterText, index === 0 && styles.selectedText]}>{item}</Text></View>)}</View>{projects.map((project) => <ProjectCard key={project.id} {...project} onPress={() => router.push(`/project/${project.id}` as never)} />)}</AppShell>; }
const styles = StyleSheet.create({ add: { color: Palette.cyan, fontFamily: FontFamily.regular, fontSize: 31 }, filters: { flexDirection: 'row', gap: 8, marginBottom: 19 }, filter: { paddingHorizontal: 12, paddingVertical: 8, borderRadius: 99, backgroundColor: '#0C294B' }, selected: { backgroundColor: '#087FFF' }, filterText: { color: '#A9C9F1', fontFamily: FontFamily.medium, fontSize: 12 }, selectedText: { color: '#FFF' } });
